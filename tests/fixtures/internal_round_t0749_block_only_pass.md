## 리뷰 (code-reviewer · 2026-08-18)

## must-fix
- 없음 (R1 accepted 5건 전부 resolved · 신규 finding 1건은 suggestion)

## 판정
판정: 통과 · finding 1건(must-fix 0건) · 확인 5건(resolved 5 / unresolved 0 / regressed 0)

회귀 실측: `python3 -m pytest tests/test_ticket_rounds.py -q` → 87 passed(R1 74 → +13) · `tests/test_engine_rev_deep_import_guard.py -q` → 98 passed(부트스트랩 byte-동일 가드 포함) · templates 3타깃 ticket_rounds.py 사본 = 엔진과 byte-동일(drift 0) · 작업 worktree git status clean.

```pm-review-v1
{"version":2,"findings":[{"id":"F-010","class":"design-proposal","severity":"suggestion","authority":"ticket_rounds.py:456-458 _is_engine_temporary · 472-473 _scan_round_files · 588-589 verify_rounds · 548-565 replace_round(finally unlink)","evidence":"점-접두 규약이 진행 중 임시 파일을 스캔 표면 밖으로 뺐고(F-001 해소) 그 대가로 **크래시 잔여**도 영구히 어떤 코드도 표면화하지 않는다. 독립 실측(리뷰 worktree 무편집·격리 tmpdir 스크립트): rounds/T-0001 에 `.01-developer.md.4242.deadbeef.tmp` 를 손으로 심어도 load_rounds=[(1,'developer',pending)] · verify_rounds=[round-pending] 뿐 · reserve_round→02-code-reviewer.md 정상이고, 잔여는 디스크에 그대로 남는다(디렉터리=['.01-developer.md.4242.deadbeef.tmp','01-developer.md','02-code-reviewer.md']). R1 이전에는 같은 잔여가 loud 였다 — 오탐이라 바꾼 것이 맞고 이 티켓의 결함은 아니다. 남는 것은 소비자 쪽 두 자리다: T-0751 lint 가 잔여를 못 보고, T-0753 이 rounds/ 를 stage 하면 git 은 점 파일도 add 하므로 board git 에 섞일 수 있다.","recommendation":"T-0751(lint)에 '점-접두 잔여 N건'을 정보 판정으로 내거나, T-0753(stage)에서 ROUND_TEMPORARY_PREFIX 항목을 제외하도록 후속 티켓에 한 줄 적어라. 이 티켓에서 코드를 고칠 필요는 없다.","design_change":false}],"confirmations":[{"id":"F-001","status":"resolved","evidence":"코드: `_temporary_round_path`(ticket_rounds.py:540-545)가 중간 파일을 점 접두로 만들고, `_is_engine_temporary`(456-458)를 `_scan_round_files`(472-473)와 `verify_rounds`(588-589) **둘 다** 건너뛴다(스캔 표면 두 곳 모두 닫힘). 테스트: tests/test_ticket_rounds.py:337-393 이 실 스레드 회수 + atomic_replace 지연 주입으로 무락 창을 만들고, 창 실재(점-접두 잔여 정확히 1건)를 **먼저 단언**한 뒤 load_rounds/verify_rounds/reserve_round 정상과 교체 후 잔여 0 을 단언한다 · 328-334 는 임시 이름이 parse_round_filename 문법 밖임을 · 396-408 은 외부 부산물(.DS_Store)도 표면 밖임을 못박는다. 민감도(격리 사본·작업 worktree 무편집): 점 접두 제거 → 87 passed → 85 passed/2 failed(temporary_round_path·harvest_window) · `_is_engine_temporary` 를 False 고정 → 85 passed/2 failed(harvest_window·stray_dot) · 둘 다 복원 후 87 passed 복귀. 독립 실측으로도 잔여를 심은 상태에서 load/verify/reserve 가 R1 재현 절차대로 정상이다."},{"id":"F-002","status":"resolved","evidence":"tests/test_ticket_rounds.py:311-325 `test_reserve_round_refuses_to_overwrite_when_the_scan_was_stale` 가 `_scan_round_files` 를 빈 목록으로 monkeypatch 해 채번이 stale 한 상황을 주입하고, reserve_round 가 RoundsError('예약 충돌')로 멈추는 것과 **기존 파일 bytes 불변**을 함께 단언한다(존재가 아니라 값 단언). 민감도: `_write_new_file`(447)의 os.O_EXCL → os.O_TRUNC → 이 테스트 1건 red('DID NOT RAISE RoundsError' · 86 passed/1 failed) — R1 시점 74/74 green 이던 공허 분기가 닫혔다. 복원 후 87 passed."},{"id":"F-003","status":"resolved","evidence":"`_safe_ticket_id`(ticket_rounds.py:328-339)가 구분자 문자 열거를 버리고 경로 규칙 판정으로 바뀌었다 — PurePosixPath/PureWindowsPath 의 name 왕복 + ':' 거부 + 후행 공백·점 거부 + 예약 장치명(219-223) 거부. tests:91-105 파라미터에 C:x·C:·T-1:a·CON·nul·LPT1·'T-0001 '·'T-0001.' 이 추가됐다. 민감도: 옛 구분자 열거 판정으로 되돌림 → 79 passed/8 failed(추가된 8 케이스 전부) · 복원 후 87 passed. false-reject 역방향도 실측했다 — board 발행 문법(board.py:3004 `T-(?:<prefix>-\\d+|\\d+)`) 표본 7종(T-0001·T-9999·T-fin-001·T-P0-001·T-service-a-012·T-123-001·T-A_b-7) 전부 통과하고, 적대 표본 16종은 전부 RoundsError 다."},{"id":"F-004","status":"resolved","evidence":"PROBLEM_DUPLICATE='round-dup'(ticket_rounds.py:209) 신설 · verify_rounds(605-612)가 순번 중복을 그 코드로 내고 detail 에 두 역할을 담는다 · 판정 코드 주석(204-206)과 verify_rounds docstring(575-578)이 '완료 게이트 red = round-gap + round-dup, 심각도는 소비자 소유'로 PM 판정과 같은 방향을 기록한다. 테스트: tests:538-545 가 코드 4종이 서로 다른 값임을(소비자가 한국어 detail 을 파싱하지 않도록) · 557-568 이 중복 → round-dup + detail 에 architect/developer 를 단언한다. 민감도: PROBLEM_DUPLICATE → PROBLEM_NAME 되돌림 → 86 passed/1 failed · 복원 후 87 passed. 독립 실측: 같은 순번 2건 → [round-dup '순번 중복 02: architect, code-reviewer'], 02 삭제 → [round-gap '순번 빈틈(삭제 의심): 02'] 로 두 코드가 분리돼 나온다."},{"id":"F-005","status":"resolved","evidence":"ROLE_LABELS 주석(ticket_rounds.py:184-188)이 '권위 방향은 이 표 → board · board 의 성장 절 표는 후속 티켓에서 이 표를 파생으로 읽는다 · 그때까지 일치는 테스트가 못박는다'로 PM 판정(라벨 권위 = ticket_rounds)을 그대로 기록하고, tests:194-198 docstring 도 같은 방향을 적은 채 board.TICKET_GROWTH_ROLE_LABELS 일치 pin 을 유지한다. PM 판정 scope 의 나머지 절반(T-0751 로의 방향 역전 명시 이월)은 보드 티켓 편집이라 이 티켓 touches(.project_manager/tools/ticket_rounds.py · tests/test_ticket_rounds.py) 밖 = PM 몫이고, developer 미이행 사항이 아니다."}]}
```
