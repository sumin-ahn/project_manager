{
  "description": "PreCompact allows compaction and asks the PM to checkpoint the interval since its last durable boundary.",
  "hooks": {
    "PreCompact": [
      {
        "matcher": "^auto$",
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s\\n' '{\"systemMessage\":\"[ctx-checkpoint] compaction 이 일어난다. 직전 박제 경계 이후 구간을 python3 .project_manager/tools/pm_log.py checkpoint --task <이름> --trigger compaction 으로 기록하고, 생성된 골격의 구간·서사 불릿을 즉시 채워라. compaction은 차단되지 않는다.\",\"suppressOutput\":false}'",
            "commandWindows": "Write-Output '{\"systemMessage\":\"[ctx-checkpoint] Compaction is starting. Record the interval since the last durable boundary with python3 .project_manager/tools/pm_log.py checkpoint --task <name> --trigger compaction, then immediately fill the interval and narrative bullets of the generated skeleton. Compaction is not blocked.\",\"suppressOutput\":false}'"
          }
        ]
      },
      {
        "matcher": "^manual$",
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s\\n' '{\"systemMessage\":\"[ctx-checkpoint] compaction 이 일어난다. 직전 박제 경계 이후 구간을 python3 .project_manager/tools/pm_log.py checkpoint --task <이름> --trigger compaction 으로 기록하고, 생성된 골격의 구간·서사 불릿을 즉시 채워라. compaction은 차단되지 않는다.\",\"suppressOutput\":false}'",
            "commandWindows": "Write-Output '{\"systemMessage\":\"[ctx-checkpoint] Compaction is starting. Record the interval since the last durable boundary with python3 .project_manager/tools/pm_log.py checkpoint --task <name> --trigger compaction, then immediately fill the interval and narrative bullets of the generated skeleton. Compaction is not blocked.\",\"suppressOutput\":false}'"
          }
        ]
      }
    ]
  }
}
