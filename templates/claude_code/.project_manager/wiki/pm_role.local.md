---
title: PM Role — 프로젝트별 오버라이드 (instance)
type: pm-role-local
---

# PM Role — 프로젝트 오버라이드

> [[pm_role]] (엔진 · `pm_update` 가 자동 갱신)의 **프로젝트별 칸**. 이 파일은 **인스턴스 소유** —
> 프레임워크 갱신이 안 건드린다(manifest 밖). 각 프로젝트가 자기 값을 채운다.
> (per-clone 가 아니라 **공유 상태** — 팀 공통이라 커밋해서 공유.)

## 보호 영역 (PM 수정 금지 — 코드 author + ADR 필요)

<!-- TODO: 예) src/core/**, 운영 config, 마이그레이션. 없으면 "없음". -->

## 사용자 게이트 (사전 동의 필수)

{{USER_GATE_ITEMS}}

<!-- ↑ PM 자율 결정 밖 — 사용자 사전 동의가 필요한 프로젝트 고유 행위로 위 토큰을 치환. 예:
  - 외부 비가역 행위 (키 발급·외부 게시·배포)
  - 유료/한도 외부 API 대량 호출
  - scope:mission ADR
  (--fill auto 면 초안 자동 생성, manual 이면 직접 치환. 없으면 "없음".) -->

## 금지 (PM·사용자 단독 불가 — 양측 합의 + ADR)

<!-- TODO: 양측 합의 + 별도 ADR 필요 영역. 예: 미션 변경, 안전 경계 약화.
없으면 절 삭제. -->
